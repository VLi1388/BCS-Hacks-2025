import Navbar from "@/components/Navbar";

const LoginPage = () => {
  return (
    <div>
      {/* TODO: Remove <Navbar /> before final release */}
      <Navbar />
    </div>
  );
};

export default LoginPage;
